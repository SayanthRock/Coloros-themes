# UI Tweaks

The UI Tweaks component is for optional, device-tested interface behavior.

## Purpose

- Status labels
- Support cards
- Device-specific UI notes
- Tested visual behavior

## Default state

Disabled until tested on a real device and software version.

## Safety rule

Never assume support across all ColorOS, OxygenOS, or realme UI versions. Mark untested behavior as `Needs test`.
