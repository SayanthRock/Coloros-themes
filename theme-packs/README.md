# Theme Packs

Place only theme assets that you created, own, or have permission to share.

Recommended check:

```bash
python3 scripts/inspect-theme-package.py /path/to/theme.theme --pretty
bash scripts/validate-module.sh
bash scripts/package.sh
```

The final module ZIP is created in `dist/`.
