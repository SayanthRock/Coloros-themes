# System UI Target

Package name: `com.android.systemui`

This folder is for preview metadata, owned visual assets, and customer status labels.

Default customer label: `Needs testing`.

Use it for APK preview screens and support reports. Keep active changes off until a matching device profile is tested and rollback is ready.
