# Settings UI Target

Package name: `com.android.settings`

This folder is for preview metadata, owned visual assets, and customer status labels for the Settings experience.

Default customer label: `Needs testing`.

Use it for APK preview screens, status pages, settings shortcuts, and support reports. Keep active changes off until a matching device profile is tested and rollback is ready.
