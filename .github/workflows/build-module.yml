name: Build Theme Module Release
run-name: Build Theme Module Release • ${{ github.event_name == 'workflow_dispatch' && inputs.version || github.ref_name }}

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: "Manual version, example: v1.0.0"
        required: false
        default: ""
      publish:
        description: "Publish GitHub Release"
        required: true
        type: boolean
        default: false

concurrency:
  group: theme-module-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  MODULE_ID: ColorOS-Themes-Rock

jobs:
  security:
    name: Security Gate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install security tools
        run: |
          sudo apt-get update
          sudo apt-get install -y git shellcheck

      - name: Run Gitleaks secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Shell script quality check
        run: |
          find scripts -type f -name "*.sh" -print0 | xargs -0 -r shellcheck

      - name: Required file integrity check
        run: |
          test -f module.prop
          test -f scripts/package.sh
          test -f scripts/validate-module.sh
          test -f scripts/check-theme-size.sh

      - name: Module validation
        run: bash scripts/validate-module.sh

      - name: Theme asset validation
        run: bash scripts/check-theme-size.sh

  build:
    name: Build Module + Attestation
    needs: security
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      id-token: write
      attestations: write
    outputs:
      module_version: ${{ steps.version.outputs.version }}
      artifact_name: ${{ steps.version.outputs.artifact_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y zip coreutils

      - name: Resolve version
        id: version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF_NAME}"
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" && "${{ inputs.version }}" != "" ]]; then
            VERSION="${{ inputs.version }}"
          else
            git fetch --tags
            LATEST=$(git tag --sort=-v:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+' | head -n 1 || true)
            [ -z "$LATEST" ] && LATEST="v0.0.0"
            IFS='.' read -r MAJOR MINOR PATCH <<< "${LATEST#v}"
            PATCH=$((PATCH+1))
            VERSION="v${MAJOR}.${MINOR}.${PATCH}"
          fi

          if [[ ! "$VERSION" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-beta)?$ ]]; then
            echo "Invalid version format: $VERSION"
            exit 1
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "artifact_name=${MODULE_ID}-${VERSION}" >> "$GITHUB_OUTPUT"
          echo "MODULE_VERSION=$VERSION" >> "$GITHUB_ENV"

      - name: Build module
        run: bash scripts/package.sh

      - name: Generate SBOM and build info
        run: |
          mkdir -p dist
          cat > dist/SBOM.txt <<EOF
          Project: ColorOS Themes Rock
          Version: ${MODULE_VERSION}
          Build runner: GitHub Actions ubuntu-latest
          Build tools: bash, zip, coreutils
          Commit: ${GITHUB_SHA}
          EOF
          cat > dist/BUILD_INFO.txt <<EOF
          Project: ${MODULE_ID}
          Version: ${MODULE_VERSION}
          Commit: ${GITHUB_SHA}
          Workflow: ${GITHUB_WORKFLOW}
          Run ID: ${GITHUB_RUN_ID}
          EOF

      - name: Generate SHA256 checksums
        run: |
          cd dist
          sha256sum * > SHA256SUMS.txt

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.version.outputs.artifact_name }}
          path: dist/*
          if-no-files-found: error
          retention-days: 30

      - name: Generate GitHub build provenance attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: 'dist/*'

      - name: Write workflow summary
        run: |
          {
            echo "## Theme Module Build"
            echo ""
            echo "- Version: ${MODULE_VERSION}"
            echo "- Artifact: ${{ steps.version.outputs.artifact_name }}"
            echo "- Build info: dist/BUILD_INFO.txt"
            echo "- SBOM: dist/SBOM.txt"
            echo "- Checksums: dist/SHA256SUMS.txt"
          } >> "$GITHUB_STEP_SUMMARY"

  release:
    name: Publish Module Release
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.publish == true)
    permissions:
      contents: write

    environment:
      name: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.outputs.artifact_name }}
          path: dist

      - name: Generate release notes
        run: |
          VERSION="${{ needs.build.outputs.module_version }}"
          cat > RELEASE_NOTES.md <<EOF
          # ColorOS Themes Rock ${VERSION}

          ## Changes
          EOF
          git log -n 20 --pretty=format:'- %s' >> RELEASE_NOTES.md || true
          cat >> RELEASE_NOTES.md <<EOF

          ## Verification
          - Gitleaks secret scan passed
          - Shell script quality check passed
          - Module and theme validation passed
          - Build info included
          - SBOM included
          - SHA256 checksums included
          - GitHub build provenance attestation generated
          EOF

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.build.outputs.module_version }}
          name: ColorOS Themes Rock ${{ needs.build.outputs.module_version }}
          body_path: RELEASE_NOTES.md
          files: dist/*
          draft: false
          prerelease: ${{ contains(needs.build.outputs.module_version, 'beta') }}
